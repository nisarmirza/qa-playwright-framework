# QA Playwright Automation Framework (UI + API + CI)

Made by Nisar Mirza
automation framework using Playwright.

## What’s included
- UI E2E tests with Page Object Model (POM)
- API smoke checks using Playwright request
- HTML report + trace/video/screenshot retained on failure
- Test tags: @smoke
- GitHub Actions CI pipeline

## Run locally
```bash
npm install
npx playwright install
npm test
npm run report
